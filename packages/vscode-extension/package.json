{
    "name": "sprawl-grid-sync",
    "displayName": "The Sprawl: Grid Sync",
    "version": "0.1.0",
    "publisher": "Atomic-Agents",
    "engines": {
        "vscode": "^1.104.0"
    },
    "activationEvents": [
        "*"
    ],
    "main": "./out/extension.js",
    "contributes": {
        "commands": [
            {
                "command": "sprawl.sync",
                "title": "Sprawl: Sync Grid",
                "icon": "$(sync)"
            }
        ]
    },
    "scripts": {
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./"
    },
    "devDependencies": {
        "@types/node": "^25.0.9",
        "@types/vscode": "^1.104.0",
        "typescript": "^5.9.3"
    }
}
