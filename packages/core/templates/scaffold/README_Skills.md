# Skills Protocol // {{HUB_NAME}}

This sector stores **Tool Definitions** and **External Integrations**.

## Usage
- Python scripts for API fetchers.
- JSON definitions for MCP Servers.
- Specialized logic that extends the agent's capabilities beyond simple text processing.

## Structure
Each skill should be in its own subfolder with a `SKILL.md` usage guide.
