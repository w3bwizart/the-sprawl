# Scripts Protocol // {{HUB_NAME}}

This sector contains **Maintenance Scripts** and **Utilities**.

## Usage
- Bash scripts for local automation (`setup.sh`, `backup.sh`).
- Python helpers that are run manually, not by the agent.

## Safety
Always audit scripts before execution.
