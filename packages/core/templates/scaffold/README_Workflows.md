# Workflows Protocol // {{HUB_NAME}}

This sector contains **Standard Operating Procedures (SOPs)** and **saved prompts**.

## Usage
- Create `.md` files here to define step-by-step instructions.
- These become available as **Slash Commands** (e.g., `/monthly-report`) if configured in `.agent/workflows`.

## Example
`onboarding.md` -> Steps to onboard a new team member to this project.
